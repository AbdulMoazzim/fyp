[project]
name = "scrum-master-agent-neon"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "AbdulMoazzim", email = "abdulmoazzam0815@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "alembic>=1.13",
    "fastapi>=0.110",
    "langchain-core>=0.3",
    "langgraph>=0.2",
    "psycopg2-binary>=2.9",
    "pydantic>=2.6",
    "python-dotenv>=1.0",
    "sqlalchemy>=2.0",
    "uvicorn>=0.29",
]

[project.scripts]
scrum-master-agent-neon = "scrum_master_agent_neon:main"

[build-system]
requires = ["uv_build>=0.12.18,<0.13.0"]
build-backend = "uv_build"
